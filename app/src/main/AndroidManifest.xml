<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.thyroidpredictor">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thyroidpredictor"
        tools:targetApi="31">

        <!-- Splash screen sebagai LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimulasiActivity" />


        <activity android:name=".OnboardingActivity" />


        <activity android:name=".DatasetActivity" />


        <activity android:name=".MainActivity" />


        <activity android:name=".TentangActivity" />

        <!-- Halaman Fitur Aplikasi -->
        <activity android:name=".FiturActivity" />

        <!-- Halaman Arsitektur Model -->
        <activity android:name=".ArsitekturActivity" />



    </application>

</manifest>
